<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ojo.vo.PostimageVO">
<select id="selectByImages" parameterType="int" resultType="io">
	select * from postimage where postnum=#{postnum}
</select>
<insert id="uploadImage" parameterType="io">
	insert into postimage(postnum, filename, filerealname, imagenum) 
	values(
	(select category from categorytbl), #{filename}, #{filerealname}, postimage_imagenum_seq.nextval)

</insert>



	

	
</mapper>














